# Core Flask dependencies (runtime)
flask>=2.0.0
flask-cors>=3.0.0
python-dotenv>=0.19.0
typing-extensions>=4.0.0
requests>=2.26.0

# Runtime ML (all inference runs on Vercel)
torch>=1.9.0
numpy>=1.20.0
pandas>=1.3.0
scikit-learn>=0.24.0
scipy>=1.7.0
sentence-transformers  # Needed for KERNEL text encoder
vaderSentiment         # Sentiment features for KERNEL

# Firebase (authentication + Firestore)
firebase-admin>=6.0.0

# Process manager (only used in non-serverless runs, harmless in runtime env)
gunicorn>=20.0.0
