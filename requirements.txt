# Core Flask dependencies (runtime)
flask>=2.0.0
flask-cors>=3.0.0
python-dotenv>=0.19.0
typing-extensions>=4.0.0

# Runtime ML (PyTorch inference + lightweight numerics)
torch>=1.9.0
numpy>=1.20.0

# Firebase (authentication + Firestore)
firebase-admin>=6.0.0

# NOTE:
# Heavy training/deep-learning extras were moved to requirements-dev.txt
# to keep Vercel builds under the memory limit.
