# Development / training extras
pandas>=1.3.0
scikit-learn>=0.24.0
scipy>=1.7.0
sentence-transformers  # Optional: KERNEL semantic embeddings (pulls transformers/torch)
vaderSentiment         # Optional: KERNEL sentiment
gunicorn>=20.0.0       # Optional: local process manager
# transformers         # Optional heavy NLP stack (commented by default)

